# 测试环境（对应env="test"）
[ENV_TEST]
base_url = http://10.68.3.106:28397  # 替换为你的测试环境地址

# 预发环境（可选，对应env="pre"）
[ENV_PRE]
base_url = http://pre-api.example.com

# 生产环境（可选，对应env="prod"）
[ENV_PROD]
base_url = http://prod-api.example.com

[DATABASE]
host = localhost
port = 3306
user = root
password = root
database = archives
charset = utf8mb4

[LOG]
log_level = INFO
log_path = ${PROJECT_ROOT}/logs/
log_max_bytes = 10485760
log_backup_count = 5